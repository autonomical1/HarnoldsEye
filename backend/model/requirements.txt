# Transformers 5.x is required so pickled classifiers (model.pkl) unpickle cleanly
# (e.g. transformers.core_model_loading present in v5, not in v4.57).
sentence-transformers>=5.3.0,<6
transformers>=5.0,<6
torch==2.11.0
scikit-learn>=1.4.0
numpy>=1.26.0,<2.2
google-generativeai>=0.8.0